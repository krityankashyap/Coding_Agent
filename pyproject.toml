[project]
name = "cluadecode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krityankashyap", email = "kashyapkrityan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.3.18",
    "langchain-groq>=1.1.3",
]

[project.scripts]
cluadecode = "cluadecode:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
